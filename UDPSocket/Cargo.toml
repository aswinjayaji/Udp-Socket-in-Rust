[package]
name = "UDPSocket"
version = "0.1.0"
authors = ["aswinjayaji <aswinjayaji@cet.ac.in>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = { version = "1.0.0", features = ["full", "tracing"] }
